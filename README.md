# FN Pro Database - Git-ready

Deploy to GitHub Pages for user 'OneWay' at: https://oneway.github.io/FN-Pro-Database/

Commands to run locally:

```bash
git init
git add .
git commit -m "Initial FN Pro Database"
git branch -M main
git remote add origin https://github.com/OneWay/FN-Pro-Database.git
git push -u origin main
```

Then enable GitHub Pages in the repository settings (branch: main, root).